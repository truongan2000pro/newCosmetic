$(document).ready(function () {
  $(".ol-content").slick({
    slidesToShow: 4,
    slidesToScroll: 1,
    dots: false,
    centerMode: true,
    focusOnSelect: true,
    // prevArrow: true,
    // nextArrow: true,
    // arrows: true,
  });
});
